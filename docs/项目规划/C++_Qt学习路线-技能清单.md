# C++ Qt 全栈开发 技能清单

> 📍 **唯一路线文件：** [`C++Qt学习路线-完善版.md`](./C++Qt学习路线-完善版.md)

本文件已整合进完善版（第 10 节：全技术栈技能清单 · 9 个维度 · V1必须/V2进阶/加分 三级），不再单独维护。

> **2026-08-12 求职校准：** 个人基线为双非本科、零实习。优先把 C++17、Qt Widgets/网络、TCP/Asio、SQL（SQLite→MySQL）、Redis 限流/受控撤销、CMake/CTest/CI/调试、联系人关系建模与权限校验、Linux 基础和高频算法提升到可独立解释、修改和验证的水平；gRPC、Protobuf、协程仍为岗位驱动的后置 spike。llfcchat 仅作为局部实现对照，规则见 [`llfcchat参考索引与对照规则.md`](./llfcchat参考索引与对照规则.md)。具体等级与证据要求见 [`求职学习计划-2026秋至2027春.md`](./求职学习计划-2026秋至2027春.md)。

---

**08-10 会话时间摘要补充证据：** 已独立完成从最后一条 `ChatMessage::send_at` 推导时间摘要：消息 UTC 时间与当前时间统一转换为本地时间后，按今天、昨天、同年和跨年规则格式化；无效时间不显示分隔符。`refreshConversationItem()` 保持仅负责 UI 文本聚合，未读清零后时间与预览不回退。人工验收、构建和 CTest 2/2 均通过，当前仍为学习中。

**当前进度（08-07，按当前项目与验收证据校准）：**

```
C++ 核心语言  ████████████████  100% ✅
STL / 标准库   ████████████████  100% ✅
现代 C++      ████████████████  100% ✅
Qt 框架       ████████████░░░░   75% (25篇笔记 · 9个Qt项目)
构建工具      ████░░░░░░░░░░░░   20% (CMake 目标、链接、CTest)
C++ 网络库    ███████████░░░░░   65% (Asio 协议、Session、strand、私聊路由、确认帧、错误回执与状态快照)
Node.js      ████████████████  100% ✅ (Express + SQLite + bcrypt + JWT Auth 服务)
项目实战      █████████░░░░░░░   58%  (ChatHub 私聊、确认、失败重试、断线 UI、会话切换、最终送达与在线状态已验收)
```

**已完成项目：** 计算器 · 番茄钟 · QQ登录界面 · 密码本 · Qt 网络练习 · Asio TCP 广播服务器 · ChatHub W8 聊天/最终送达/在线状态切片（仍在巩固）

**08-07 补充证据：** ChatHub 已完成一轮稳定性优化：发送前预建消息模型、`local_id` 错误关联、TCP 建连超时、密钥启动检查、HTTP 输入边界和协议回归测试均已验证；相关能力仍标注为学习中，待独立迁移后再评定掌握度。认证响应阶段的独立超时仍列为 W10 待补边界。

**08-09 补充证据：** 已在不改协议和业务行为的前提下完成 ChatHub 内部模块化整理：协议常量、聊天模型、客户端通信层、UI 与服务端会话职责更清晰；构建成功且 CTest 2/2 通过。当前仍为学习中，待独立完成 `ChatMessage` 直传接口改造后再评定掌握度。

**08-10 补充证据：** 已独立完成收到消息的 `ChatMessage` 直传改造：`type=1` 正文先校验必填字段与有效 ISO 时间，再由客户端组装模型并经信号传给 UI；双客户端显示和非法时间拒绝已人工验收，构建成功。当前仍为学习中，待迁移到发送与状态更新路径后再评定掌握度。

**08-10 W8-8 阶段证据：** 已独立完成 `QListWidget` 会话项的稳定身份置顶和本地发送路径迁移：仅用 `Qt::UserRole` 定位，`takeItem()` 后立刻 `insertItem()` 归还所有权；写入完整消息后才排序，纯显示和状态更新不改变顺序。Bob/Carol 连续发送人工验收、构建和 CTest 2/2 均通过；当前仍为学习中，待迁移接收与防御性补建路径后再评定。

**08-10 W8-8 接收证据：** 已独立完成接收消息路径迁移：用 `message.from` 保持对方会话身份，当前会话接收不累加未读，非当前会话接收置顶并正确累加未读。人工验收、构建和 CTest 2/2 均通过；当前仍为学习中，待完成防御性补建路径后再评定。

**08-10 W8-8 完成证据：** 已独立完成防御性补建路径与会话最近消息置顶整体验收：已有 `local_id` 仅恢复 `Sending` 状态，找不到时才补建、置顶和刷新。模型首次写入驱动排序、显示和状态更新不排序的边界已清晰；人工验收、构建和 CTest 2/2 均通过，当前仍为学习中。

**08-10 发送链路补充证据：** 已完成发送消息的模型直传与失败关联修复：UI 在网络调用前创建并保存具有稳定 `local_id` 和 UTC 时间的消息，通信层仅编码和发送；断线失败、原消息重试、手填接收者和会话回退均已人工验收。当前仍为学习中，待迁移确认与失败状态更新接口后再评定掌握度。

**08-10 状态更新补充证据：** 已完成确认与失败状态的模型直传：确认/失败仅携带定位和状态字段，UI 只更新既有会话消息；未知 ID 不创建气泡，无 ID 错误仍按认证或通用错误处理。构建、CTest 与人工验收通过，当前仍为学习中。

**08-10 UI 渲染补充证据：** 已独立完成消息气泡接口的 `ChatMessage` 直传：渲染函数不再接收并列字段，重试路径通过 `local_id` 回查完整会话模型。构建与正常消息、失败红点、重试的人工验收均通过；当前仍为学习中。

**08-10 回调依赖补充证据：** 已独立完成失败重试回调的最小捕获：Lambda 只保存 `local_id`，不持有完整消息或容器元素引用；构建、失败红点和重试人工验收通过。当前仍为学习中。

**08-10 会话身份补充证据：** 已独立用 `Qt::UserRole` 保存会话 peer，展示文本不再承担身份职责；同时修复消息查询返回局部副本地址的生命周期风险。Bob/Carol 多会话与重复会话去重已人工验收，未读计数仍为学习中。

**08-10 未读计数补充证据：** 已独立完成 `m_unreadCounts` 会话状态模型：非当前会话消息按 `message.from` 递增并刷新同一列表项，点击会话后按 `UserRole` 清零。Bob `(2)`、点击清零和当前会话直显均已人工验收；构建与 CTest 2/2 通过，当前仍为学习中。

**08-10 会话预览补充证据：** 已独立完成从 `m_conversations` 推导最后消息摘要：换行规范化、固定长度截断、代理对保护，以及“名称/未读数/预览”的两行展示。三条新增消息路径均会刷新对应 peer；本地发送、当前/非当前接收、未读清零与回归功能均已人工验收，构建与 CTest 2/2 通过，当前仍为学习中。

**08-11 W8-9 阶段证据：** 已独立完成最终送达回执链路：新增 `delivery_receipt=7` 与 `Delivered` 状态，服务端按接收者认证身份维护短期 `PendingDeliveryMap` 并在回执后单次消费；B 在消息模型和界面处理完成后回执，A 仅按 `local_id` 更新已有消息，不创建气泡、不改变未读或会话顺序。双客户端人工验收成功，构建和 CTest 2/2 通过。当前仍为学习中，待独立迁移“认证归属校验 + 短期状态索引”的模式后再评定掌握度。

**08-12 W8-10 阶段证据：** 已完成 `online_users=8` 全量状态快照：Server 仅从认证用户名映射构建稳定排序名单，在 Server strand 上处理上线、真实下线与同名新连接接管；ChatClient 严格校验 JSON 并缓存最后有效快照，MainWindow 在连接信号后回填缓存、过滤本人并用 `Qt::UserRole` 实现仅回填接收者的点击行为。双客户端上线/下线、私聊回归与同名登录接管验收成功，构建和 CTest 2/2 通过；当前为学习中，待独立迁移“快照状态 + 视图回填”模式后评定掌握度。
